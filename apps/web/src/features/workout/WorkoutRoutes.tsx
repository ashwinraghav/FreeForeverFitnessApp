/**
 * Entry point for the workout feature. OWNED BY THE workout TEAM.
 *
 * The shell renders this behind its route and code-splits it. Build the
 * feature's own routing inside here; do not edit src/app/routes.ts.
 */
export default function WorkoutRoutes() {
  return <div style={{ padding: 'var(--ff-space-20)' }}>Workout — not built yet.</div>;
}
