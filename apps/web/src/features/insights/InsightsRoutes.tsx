/**
 * Entry point for the insights feature. OWNED BY THE insights TEAM.
 *
 * The shell renders this behind its route and code-splits it. Build the
 * feature's own routing inside here; do not edit src/app/routes.ts.
 */
export default function InsightsRoutes() {
  return <div style={{ padding: 'var(--ff-space-20)' }}>Insights — not built yet.</div>;
}
