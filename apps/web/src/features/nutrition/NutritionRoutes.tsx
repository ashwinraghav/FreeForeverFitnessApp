/**
 * Entry point for the nutrition feature. OWNED BY THE nutrition TEAM.
 *
 * The shell renders this behind its route and code-splits it. Build the
 * feature's own routing inside here; do not edit src/app/routes.ts.
 */
export default function NutritionRoutes() {
  return <div style={{ padding: 'var(--ff-space-20)' }}>Nutrition — not built yet.</div>;
}
