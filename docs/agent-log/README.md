# Agent log

Redacted transcripts of the AI sessions that built this project (ADR-0019).

Written by a redaction script — home-directory paths rewritten, secret-pattern scanned, and
manually gated before first publish. Never a raw dump.

_Empty until the redaction pipeline lands._
