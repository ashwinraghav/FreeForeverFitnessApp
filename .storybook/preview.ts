/**
 * Shared, not copied. The design system owns how a story is presented — theme
 * decorators, backgrounds, the a11y panel — and publishes it as
 * `@freeforever/design-system/storybook`. Re-exporting keeps this gallery and the
 * package's standalone one from drifting apart.
 */
export { default } from '@freeforever/design-system/storybook';
